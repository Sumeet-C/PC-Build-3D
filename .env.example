# MongoDB connection URI (optional - app uses in-memory fallback if not set)
MONGODB_URI=mongodb://localhost:27017/pc-builder-india

# Server port
PORT=4000

# JWT secret key for authentication
JWT_SECRET=your_super_secret_jwt_key_change_in_production
